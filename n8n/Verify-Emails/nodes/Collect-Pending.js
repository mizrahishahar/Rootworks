const baseId=$('Params In').first().json['Clayroots Base ID']; const tableId=$('Resolve Table').first().json.tableId; const out=[]; for(const it of $input.all()){ const v=it.json; if(v.Status==='verifying' && v._pendEmail){ out.push({ json:{ rowId:v.id, email:v._pendEmail, slot:v._pendSlot, tableId, baseId } }); } } return out;