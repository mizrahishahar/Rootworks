# OS files
.DS_Store
Thumbs.db
desktop.ini

# Obsidian system files
.obsidian/
.trash/

# Scripts
.scripts/

# Backup / editor cruft
*.bak
*~
.mcp.json

# Logs — session logs & synced client comms (mirror Vault; kept out of git, synced via Drive)
Logs/

# NOTE: .claude/ IS tracked here (unlike the Vault) so the operating system is versioned.
