const dump=(n)=>{ try{ const items=$(n).all().map(i=>i.json); const first=items[0]||null; let meta={}; if(first&&typeof first==='object'&&!Array.isArray(first)){ meta.topKeys=Object.keys(first); const arr=first.data||first.emails; if(Array.isArray(arr)){ meta.arrLen=arr.length; meta.msgKeys=arr[0]?Object.keys(arr[0]):[]; meta.sample=arr.slice(0,3).map(m=>({lead:m.lead,direction:m.direction,current_step:m.current_step,ts:m.sent_on||m.timestamp_created||m.created_at,subject:String(m.subject||'').slice(0,40),ws:m.workspace_id})); } } return { count: items.length, meta, first: JSON.stringify(first).slice(0,700) }; }catch(e){ return { err: String(e) }; } };
return [{ json: { A_received: dump('A Range Full'), B_wsonly: dump('B Range Narrow'), C_campemails: dump('C No Dates'), D_summary: dump('D US Dates') } }];