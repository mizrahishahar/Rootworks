To finish this session off, capture what just happened in a session and write it to the client's log.

Do this like this:

1. Run this command EXACTLY to create a new log from our template: `obsidian vault="The Vault" command "Templater: Create Client Log"`
2. Fill the log based on the session and make sure you update everything important! This is for your future context. So make sure you combine efficiency (only what's matters) with not forgetting to mention important stuff and updates
3. Move it into the appropriate `Prospects/{client}/Logs/` of the client